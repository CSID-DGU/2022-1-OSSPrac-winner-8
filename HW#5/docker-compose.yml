version: "3.7"

services:
  web:
    build: .
    image: ossprac
    volumes:
      - "./app:/app"
      - /var/run/docker.sock:/var/run/docker.sock
      - /usr/bin/docker:/usr/bin/docker
    ports:
      - "5000:80"
